# YOU GYM UI 设计规范

版本：1.0  
更新日期：2026-08-14  
适用范围：iOS、Android、Web 开发预览及管理端中的移动端预览

本规范来自当前 `Fitness Neon V2` 方案。整体风格是纯黑运动舞台、深灰玻璃界面、荧光黄绿操作色和洋红肌肉高亮色。所有新页面必须先完整阅读本文件，再开始布局或视觉设计。

首版视觉真源为 `UI/ChatGPT Image 2026年8月14日 17_27_33 (1).png` 与 `UI/ChatGPT Image 2026年8月14日 17_27_34 (2).png`。首版人体模块使用可交互 2D 正反面人体素材，不依赖 Figma 或 Unity。

## 1. 执行原则

1. 只使用本文定义的颜色、字体、间距和组件参数，不在单个页面中临时创造相近色值。
2. 荧光黄绿色表示操作、选中和聚焦状态；洋红与暖红表示肌肉、运动热区和身体数据，两者不可互换。
3. 2D 人体选择器是无边框主舞台，不放进装饰卡片中。控件可以浮在舞台上，但不能遮挡当前高亮肌肉。
4. 常规页面使用 20px 水平安全边距；底部胶囊导航使用 16px 外边距。
5. 不使用装饰性光球、无意义渐变或大面积橙色主题。渐变只允许出现在动作媒体缩略图、数据可视化或人体热区中。
6. 不允许卡片嵌套卡片。底部面板、列表行和独立功能卡片应保持清晰的单层结构。
7. 如业务确实需要新增 token 或例外，必须先更新本文件，再应用到页面。

### 1.1 参考设计的使用规则

- Apple Fitness 仅参考数据层级、运动氛围、清晰留白和完成反馈；不复制其活动圆环、页面构图、商标、图标或专有文案。
- Workout.cool 仅参考“场地/器械 → 肌肉 → 动作”的分步任务结构、计划卡信息顺序和创建/浏览分离；不复制其品牌、广告、Premium、排行榜、页面截图、代码或内容库。
- YOU GYM 的最终视觉始终以本文件 Fitness Neon V2 token 为准，参考产品不能引入新的主色、圆角体系或导航结构。
- 第三方截图只能放入 Figma `99_References` 页面并标记为不可交付；正式页面必须使用原创布局、原创文案和已授权资产。

## 2. 配色

### 2.1 核心颜色

| Token | 色值 | 用途 |
| --- | --- | --- |
| `color-primary` | `#B3FF00` | 主按钮、选中态、聚焦状态、活动导航项 |
| `color-primary-soft` | `rgba(179, 255, 0, 0.16)` | 数量徽标、轻量选中背景 |
| `color-primary-border` | `rgba(179, 255, 0, 0.24)` | 黄绿色徽标和轻量状态边框 |
| `color-muscle` | `#FF2D55` | 目标肌肉、运动热区、身体数据强调 |
| `color-muscle-warm` | `#FF4B3E` | 肌肉参与度渐变中的暖色端点 |
| `color-marker` | `#35E6E8` | 3D 坐标点、准星中心和空间定位提示 |
| `color-success` | `#32D74B` | 完成、同步成功和安全状态 |
| `color-warning` | `#FFD60A` | 风险提示、接近目标和需注意状态 |
| `color-error` | `#FF453A` | 表单错误、失败和危险操作 |
| `color-info` | `#64D2FF` | 信息提示、帮助和系统状态 |

### 2.2 背景与表面

| Token | 色值 | 用途 |
| --- | --- | --- |
| `color-bg-canvas` | `#000000` | 页面和 3D 舞台背景 |
| `color-bg-sheet` | `rgba(10, 10, 13, 0.985)` | 底部动作面板 |
| `color-surface-card` | `rgba(23, 23, 27, 0.97)` | 信息卡片、肌肉摘要卡片 |
| `color-surface-control` | `rgba(27, 27, 30, 0.98)` | 下拉框、筛选项、浮动控件 |
| `color-surface-nav` | `rgba(26, 26, 29, 0.96)` | 底部胶囊导航 |

### 2.3 文字与边界

| Token | 色值 | 用途 |
| --- | --- | --- |
| `color-text-primary` | `#FFFFFF` | 页面标题、卡片标题、主要数值 |
| `color-text-secondary` | `#A3A3AD` | 正文、说明文字、辅助信息 |
| `color-text-tertiary` | `#8F8F9C` | 未选中导航、次要标签 |
| `color-text-inverse` | `#050505` | 黄绿色按钮和选中项上的文字与图标 |
| `color-border-default` | `rgba(255, 255, 255, 0.10)` | 卡片、导航和面板边框 |
| `color-border-strong` | `rgba(255, 255, 255, 0.16)` | 浮动工具按钮边框 |
| `color-divider` | `rgba(255, 255, 255, 0.08)` | 列表分隔线 |

### 2.4 人体模型专用颜色

| Token | 色值 | 用途 |
| --- | --- | --- |
| `color-anatomy-stage` | `#232323` | 人体选择器和 3D 视口的中性深灰舞台 |
| `color-anatomy-base` | `#9AA8BB` | 未选中肌肉主体材质 |
| `color-anatomy-depth` | `#6F7D91` | 深层结构、背光面和体积阴影 |
| `color-anatomy-neutral` | `#777B80` | 头部、手足和非训练结构 |
| `color-anatomy-outline` | `#F5F7FA` | 人体外轮廓和肌肉分区线 |

人体专用颜色只用于模型本身，不能替代页面主色。目标肌肉仍使用 `color-muscle`，空间定位仍使用 `color-marker`。

### 2.5 用色规则

- 页面主背景必须为 `color-bg-canvas`。
- 每个视图最多同时出现一个主要黄绿色操作区域，避免荧光色失去重点。
- 肌肉高亮必须配合名称、轮廓或准星，不能只依赖颜色表达选中状态。
- 大段正文禁止使用纯白，使用 `color-text-secondary` 降低视觉噪声。
- 禁用状态通过透明度表达：内容整体透明度为 `0.36`，不新增灰色色值。

## 3. 字体

### 3.1 字体家族与字重

- 默认字体：`Noto Sans`。
- 跨 iOS 与 Android 均使用同一字体文件，避免不同系统字体造成布局偏差。
- 常规正文使用 `Medium / 500`，标题和操作文字使用 `Bold / 700`。
- 字间距固定为 `0`，不使用负字间距，也不随屏幕宽度缩放字号。

### 3.2 字体层级

| 样式 | 字号 | 字重 | 行高 | 用途 |
| --- | ---: | ---: | ---: | --- |
| `type-page-title` | 32px | 700 | 40px | 页面主标题，如“人体探索” |
| `type-section-title` | 24px | 700 | 32px | 底部面板、主要模块标题 |
| `type-card-title` | 20px | 700 | 28px | 肌肉摘要和重点卡片标题 |
| `type-list-title` | 16px | 700 | 22px | 动作名称和列表主信息 |
| `type-button` | 15px | 700 | 20px | 主按钮文字 |
| `type-body` | 14px | 500 | 20px | 常规正文和表单内容 |
| `type-supporting` | 13px | 500 | 18px | 页面副标题和组件辅助信息 |
| `type-caption` | 12px | 500 | 18px | 动作元数据、时间和说明 |
| `type-eyebrow` | 10px | 700 | 15px | 英文状态、卡片眉题、导航标签 |

### 3.3 排版规则

- 页面标题最多一行；超长标题应修改文案，不缩小到 28px 以下。
- 正文默认左对齐，数字指标可以右对齐或使用等宽数字特性。
- 标题使用 `color-text-primary`；说明文字使用 `color-text-secondary`；英文状态使用 `color-primary`。
- 紧凑组件内的标题与正文间距为 4px，页面标题与副标题间距为 2–4px。

## 4. 间距

### 4.1 基础间距系统

所有间距必须取自 4px 基础网格。

| Token | 数值 | 常见用途 |
| --- | ---: | --- |
| `space-1` | 4px | 标题与说明、图标微调 |
| `space-2` | 8px | 筛选项间距、紧凑组件间距 |
| `space-3` | 12px | 图标与文字、列表内部间距 |
| `space-4` | 16px | 卡片内边距、普通元素间距 |
| `space-5` | 20px | 页面水平边距、面板水平边距 |
| `space-6` | 24px | 模块间距、标题区与内容区间距 |
| `space-8` | 32px | 大模块分区 |
| `space-10` | 40px | 页面级视觉分区 |
| `space-12` | 48px | 大面积舞台或空状态留白 |

### 4.2 页面布局规则

- 标准设计基准画板为 `390 × 844`，同时检查 `360 × 800` 等 Android 尺寸。
- 页面左右边距：20px。
- 底部胶囊导航左右边距：16px。
- 页面主要模块间距：24px；同一模块内元素间距：8–16px。
- 卡片内边距：16px；底部面板顶部内容边距：28px。
- 列表缩略图与文字间距：12px；列表行高度：82px。
- 所有可点击区域最小为 `44 × 44px`，图标可小于点击热区。
- 安全区由系统 Insets 控制，不使用固定像素模拟刘海或底部手势区。

## 5. 组件样式

### 5.1 主按钮

- 高度：48px。
- 圆角：14px。
- 背景：`color-primary`。
- 文字：`type-button` + `color-text-inverse`。
- 水平内边距：20px。
- 阴影：`0 8px 20px rgba(117, 204, 0, 0.24)`。
- 按下态：整体透明度 `0.86`，不改变色相。
- 禁用态：整体透明度 `0.36`，移除阴影。

### 5.2 筛选胶囊与次级按钮

- 高度：34px。
- 圆角：17px。
- 水平内边距：12px；垂直内边距：7px。
- 默认背景：`color-surface-control`。
- 默认边框：1px `color-border-default`。
- 默认文字：13px / 700 / 18px，`color-text-secondary`。
- 选中背景：`color-primary`；选中文字：`color-text-inverse`；选中态不显示边框。

### 5.3 下拉导航

- 高度：48px，宽度填满内容区。
- 圆角：24px。
- 内边距：左右 16px。
- 背景：`color-surface-control`。
- 边框：1px `rgba(255, 255, 255, 0.11)`。
- 阴影：`0 8px 18px rgba(0, 0, 0, 0.32)`。
- 标签：14px / 500 / 20px，`color-text-primary`。
- 展开图标：18px，靠右对齐。

### 5.4 信息卡片

- 标准圆角：20px。
- 背景：`color-surface-card`。
- 边框：1px `color-border-default`。
- 内边距：16px。
- 阴影：`0 12px 28px rgba(0, 0, 0, 0.42)`。
- 可使用 24px 背景模糊；低性能 Android 设备使用不透明 `#17171B` 回退。
- 同一个卡片内最多包含一个主要操作，禁止嵌套另一张卡片。

### 5.5 底部动作面板

- 顶部圆角：26px；底部贴合屏幕，不加圆角。
- 背景：`color-bg-sheet`。
- 顶部边框：1px `color-border-default`。
- 背景模糊：30px。
- 阴影：`0 -14px 34px rgba(0, 0, 0, 0.62)`。
- 顶部拖拽条：36 × 5px，圆角 3px，白色 58% 透明度。
- 内容左右边距：20px；首个标题距离顶部 28px。

### 5.6 底部胶囊导航

- 页面左右边距：16px。
- 高度：58px。
- 圆角：29px。
- 背景：`color-surface-nav`。
- 边框：1px `color-border-default`。
- 背景模糊：22px。
- 阴影：`0 10px 24px rgba(0, 0, 0, 0.48)`。
- 一级导航项等宽分布，图标尺寸 20–24px，标签使用 `type-eyebrow`。
- 选中项图标与标签使用 `color-primary`；未选中项使用 `color-text-tertiary`。
- 不在胶囊导航内部再放独立卡片背景。

### 5.7 浮动图标按钮

- 点击热区：48 × 48px。
- 形状：圆形，圆角 24px。
- 背景：`color-surface-control`。
- 边框：1px `color-border-strong`。
- 图标：16–22px，白色 92% 透明度。
- 多个按钮垂直排列时，间距为 4px。
- 整组阴影：`0 10px 22px rgba(0, 0, 0, 0.42)`。

### 5.8 动作列表行

- 行高：82px。
- 缩略图：56 × 56px，圆角 8px。
- 缩略图与文本间距：12px。
- 标题：`type-list-title`。
- 元数据：`type-caption` + `color-text-secondary`。
- 右侧箭头：18px，`color-text-tertiary`。
- 分隔线：1px `color-divider`，从文字区域开始，不贯穿缩略图。

### 5.9 状态徽标

- 推荐尺寸：最小 30 × 28px。
- 圆角：14px。
- 背景：`color-primary-soft`。
- 边框：1px `color-primary-border`。
- 文字：12–13px / 700，`color-primary`。

## 6. 2D 人体模块规则

- 人体素材采用“解剖插画”视觉：冷灰蓝主体、清晰白色外轮廓与肌肉分区线、克制的体积阴影，不使用肉色皮肤、写实毛发、油亮材质或夸张健美比例。
- 默认状态并排展示同一性别的正面与背面全身图；选中肌肉后，相关视角通过缩放和位移成为主图，另一视角淡出或退到辅助位置。
- 人体素材保持无边框、全舞台展示，背景使用 `color-anatomy-stage` 或纯黑，不使用亮色径向渐变。
- 黄绿色用于交互反馈、目标状态文字和准星外圈；洋红与暖红用于肌肉组织高亮；青色用于空间中心点。
- 用户选中肌肉后，2D 画布缩放和位移动画建议为 450–600ms，缓动使用 `ease-in-out`。
- 肌肉高亮必须同时显示三级路径或更细层级，例如“胸部 · 胸大肌 · 上束”。
- 工具按钮默认收纳在舞台右侧，不与人体关键区域重叠。
- 从人体舞台进入动作筛选时，使用底部面板承接，再进入保留目标肌肉上下文的筛选页面。
- 首版正式资源使用透明背景男性/女性正面与背面人体图，配合独立 SVG/JSON 热区；位图负责视觉，热区负责命中和无障碍语义。

## 7. 新页面交付检查

每个新页面完成前必须逐项检查：

- 已完整阅读本文件，并只使用已定义 token。
- 390 × 844 与 360 × 800 下无文字、按钮或导航重叠。
- 页面标题、正文和辅助文字使用正确的字号层级。
- 所有点击热区至少为 44 × 44px。
- 主要操作只有一个明确的黄绿色视觉焦点。
- 卡片、面板和导航没有不必要的嵌套。
- 肌肉或状态信息不只依赖颜色表达。
- 文字没有裁切，最长中文文案可以正常换行。
- iOS 与 Android 均验证安全区和底部手势区域。

## 8. 响应式布局与安全区

### 8.1 设计尺寸

| 类型 | 基准尺寸 | 用途 |
| --- | --- | --- |
| `phone-compact` | `360 × 800` | 小尺寸 Android 验证 |
| `phone-base` | `390 × 844` | Figma 主设计画板 |
| `phone-large` | `430 × 932` | 大尺寸 iPhone / Android 验证 |
| `tablet-preview` | `768 × 1024` | 平板兼容预览，不作为首版主设计尺寸 |

### 8.2 响应规则

- 字号不随屏幕宽度缩放，使用固定字体 token。
- 页面水平边距在手机端固定为 20px；宽度不足时优先让内容换行，不压缩到 16px 以下。
- 内容宽度使用 `screenWidth - 40px`，底部胶囊导航宽度使用 `screenWidth - 32px`。
- 列表和表单使用单列布局；平板可将统计卡片改为双列，但训练执行页和人体舞台仍保持单一主焦点。
- 3D 人体舞台高度使用剩余可用空间，必须保证人体主要轮廓完整可见。
- 屏幕高度不足时，优先让内容区域滚动；状态栏、页面标题和主操作不可相互覆盖。
- 键盘弹起后，当前输入框和提交按钮必须可见；使用键盘避让，不通过缩小字体解决。

### 8.3 安全区

- 顶部内容从系统 `safeAreaInsets.top` 开始布局，再增加 8–12px 视觉间距。
- 底部固定控件需要加上 `safeAreaInsets.bottom`，胶囊导航本体高度仍为 58px。
- 底部面板的内容底部内边距为 `max(20px, safeAreaInsets.bottom + 8px)`。
- 不在 Figma 中把刘海、灵动岛或系统手势条作为业务组件；它们只用于设备预览。

## 9. 完整组件规范

### 9.1 顶部页面栏

- 高度由安全区和内容决定，不使用固定 64px 强制值。
- 品牌眉题：12px / 700 / 15px，白色。
- 页面标题：`type-page-title`，标题顶部与品牌眉题间距 6px。
- 页面副标题：`type-supporting`，与标题间距 2px。
- 右侧最多放置 1 个主要图标按钮或头像入口。
- 返回按钮使用 44 × 44px 热区、20px 图标，不使用带文字的圆角矩形返回按钮。

### 9.2 文本输入框

- 最小高度：52px。
- 圆角：14px。
- 背景：`color-surface-control`。
- 默认边框：1px `color-border-default`。
- 水平内边距：16px。
- 输入文字：14px / 500 / 20px，`color-text-primary`。
- 占位文字：14px / 500 / 20px，`color-text-tertiary`。
- 标签：12px / 700 / 18px，位于输入框上方 8px。
- 聚焦态：1.5px `color-primary` 边框，不增加外发光。
- 错误态：1px `color-error` 边框；错误说明位于下方 6px，使用 12px / 500 / 18px。
- 禁用态：整体透明度 0.36，不接受点击。
- 密码、验证码和搜索输入必须使用对应熟悉图标，不用文字模拟图标。

### 9.3 手机号与验证码输入

- 手机号输入使用数字键盘，格式展示为 `138 0000 0000`，提交前移除空格。
- 验证码使用单个 52px 高输入框或 6 个等宽字符槽；同一项目中只能选择一种模式。
- “获取验证码”是输入框右侧文字操作，使用 `color-primary`，点击热区至少 44px。
- 倒计时使用 `59s` 格式和 `color-text-tertiary`，倒计时期间不可重复发送。
- 错误状态必须给出原因，例如“验证码已过期”，不能只显示红色边框。

### 9.4 搜索框

- 高度：44px。
- 圆角：14px。
- 背景：`color-surface-control`。
- 左侧搜索图标：18px；图标与文字间距 8px。
- 右侧清除按钮热区：44 × 44px，仅在有输入时显示。
- 搜索建议列表与输入框间距：8px，不嵌套进搜索框背景中。

### 9.5 分段控制器

- 容器高度：40px，圆角 20px。
- 容器背景：`color-surface-control`。
- 内边距：3px。
- 选中项背景：`color-text-primary`；选中文字：`color-text-inverse`。
- 未选中文字：`color-text-secondary`。
- 每项等宽，最小点击宽度 64px。
- 用于男女、正背面、浅层/深层等互斥模式；不可用于多选。

### 9.6 标签页

- 顶部标签页使用 34px 高胶囊；样式复用筛选胶囊。
- 内容视图标签使用下划线时，下划线高 2px、宽度随文字、颜色为 `color-primary`。
- 一级模块不得使用顶部标签替代底部导航。
- 标签超过 4 个时允许横向滚动，并在右侧保留 20px 内容提示。

### 9.7 复选框与单选框

- 可见控件：22 × 22px；点击热区至少 44 × 44px。
- 默认边框：1.5px `color-border-strong`。
- 选中背景：`color-primary`；选中图标：`color-text-inverse`。
- 复选框圆角 6px；单选框为圆形。
- 文字与控件间距：10px。

### 9.8 开关

- 尺寸：44 × 26px。
- 圆角：13px。
- 关闭背景：`color-surface-control`，边框 `color-border-default`。
- 开启背景：`color-primary`。
- 滑块：22 × 22px；开启时使用 `color-text-inverse`，关闭时使用白色。
- 切换动画：200ms，`motion-standard`。

### 9.9 数值步进器

- 总高度：44px。
- 减号和加号按钮热区各 44 × 44px，中间数值宽度至少 64px。
- 图标按钮使用减号、加号图标，不使用“减少”“增加”文字按钮。
- 重量步进默认 2.5kg；次数步进默认 1；休息时间步进默认 15 秒。
- 长按可以连续变化，但必须显示当前值。

### 9.10 滑块

- 轨道高度：4px；未完成轨道使用 `color-surface-control`，已完成轨道使用 `color-primary`。
- 拖动点：24 × 24px，白色填充，2px `color-primary` 边框。
- RPE、训练强度等滑块必须同时显示离散数值，不能只依赖位置。

### 9.11 对话框

- 手机端宽度：`screenWidth - 40px`，最大 350px。
- 圆角：20px。
- 背景：不透明 `#17171B`，避免多层模糊造成可读性下降。
- 内边距：20px。
- 标题：20px / 700 / 28px；正文：14px / 500 / 20px。
- 操作区与正文间距：20px。
- 危险操作使用 `color-error` 文字或边框，不使用荧光黄绿。
- 两个操作横向排列；三个以上操作改为底部动作面板。

### 9.12 Toast 与行内提示

- Toast 位于底部导航上方 16px，最长显示 3 秒。
- 背景：`#232327`；圆角 12px；水平内边距 14px，垂直内边距 10px。
- 普通提示使用白色文字；成功、警告、错误使用对应状态图标和颜色。
- 表单错误优先使用行内提示，不用 Toast 替代字段错误。

### 9.13 加载状态

- 首屏加载使用结构化骨架屏，不在完整页面中央只放一个旋转图标。
- 骨架底色：`#17171B`；高光：`rgba(255,255,255,0.06)`。
- 按钮提交中保留按钮宽高，文字替换为 18px 进度图标，避免布局跳动。
- Unity 模型加载显示下载进度、资源层级和“使用基础模型继续”入口。

### 9.14 空状态

- 空状态包含：20–24px 图标、20px 标题、13px 说明和一个明确操作。
- 图标使用 `color-text-tertiary`，主要操作使用 `color-primary`。
- 不使用大面积插画占据训练工具页面。
- 文案说明当前状态和下一步，例如“还没有训练记录，完成一次训练后这里会显示趋势”。

### 9.15 错误状态

- 网络错误、权限错误、内容为空和服务异常必须使用不同文案。
- 错误图标使用 `color-error`；重试按钮仍使用 `color-primary`。
- Unity 资源失败时保留基础人体模型和文字导航，不展示空白舞台。
- 错误页面必须保留返回路径或底部导航。

### 9.16 训练组记录行

- 行高：56px。
- 列顺序：组序号、重量、次数/时间、RPE、完成状态。
- 表头使用 10px / 700 / 15px，`color-text-tertiary`。
- 数值输入最小宽度 52px，居中对齐。
- 完成按钮为 32 × 32px 圆角方形；完成后背景为 `color-primary`，勾选图标为黑色。
- 当前组使用 2px 左侧黄绿指示条，不给整行铺满荧光背景。
- 上次记录使用 12px 辅助文字显示在当前输入下方。

### 9.17 休息计时器

- 主数字：48px / 700 / 56px，居中显示。
- 环形进度轨道：8px，进行中使用 `color-primary`，背景轨道使用 `#2A2A2E`。
- 操作包括减 15 秒、跳过、加 15 秒；减号和加号使用图标按钮。
- 剩余 10 秒使用轻触觉反馈；不能仅通过闪烁传达。
- 锁屏或切到后台后，按真实时间校正，不按帧累计。

### 9.18 数据图表

- 图表背景透明，不额外套白色或高对比卡片。
- 主数据线：2px `color-primary`；肌肉/训练量数据可使用 `color-muscle`。
- 网格线：1px `color-divider`。
- 轴文字：10px / 500 / 15px，`color-text-tertiary`。
- 柱状图柱宽 6–12px，柱间距至少等于柱宽。
- 选中数据点使用 8px 实心点和 1px 白色外圈。
- 图表必须提供文字摘要，例如“较上周增加 8%”，不能只显示图形。

### 9.19 营养指标卡

- 热量使用 `color-primary`。
- 蛋白质使用 `color-muscle`。
- 碳水使用 `color-marker`。
- 脂肪使用 `color-warning`。
- 四项颜色语义固定，所有饮食页面和图表保持一致。
- 卡片标题使用 12px，数值使用 20–24px，单位使用 12px。
- 进度超过目标时不改变布局，改用状态文案和警告图标。

## 10. 圆角、边框和阴影系统

### 10.1 圆角 token

| Token | 数值 | 用途 |
| --- | ---: | --- |
| `radius-xs` | 6px | 复选框、小标签 |
| `radius-sm` | 8px | 媒体缩略图、紧凑元素 |
| `radius-md` | 12px | Toast、轻量容器 |
| `radius-lg` | 14px | 输入框、主按钮 |
| `radius-card` | 20px | 信息卡片、对话框 |
| `radius-sheet` | 26px | 底部面板顶部 |
| `radius-pill` | 999px | 胶囊筛选和圆形按钮 |

常规卡片统一使用 20px，不能在同一页面同时出现 16、18、20、22px 多套近似圆角。

### 10.2 边框规则

- 暗色界面边框用于分离层次，不用于装饰。
- 默认边框为 1px `color-border-default`。
- 当前聚焦控件使用 1.5px `color-primary`。
- 列表分隔线从文字内容起始位置绘制，不穿过缩略图。
- 相邻表面已有明显明度差时可以不加边框。

### 10.3 阴影 token

| Token | 参数 | 用途 |
| --- | --- | --- |
| `shadow-control` | `0 8px 18px rgba(0,0,0,0.32)` | 下拉框和悬浮控件 |
| `shadow-card` | `0 12px 28px rgba(0,0,0,0.42)` | 信息卡片 |
| `shadow-nav` | `0 10px 24px rgba(0,0,0,0.48)` | 底部导航 |
| `shadow-sheet` | `0 -14px 34px rgba(0,0,0,0.62)` | 底部面板 |
| `shadow-primary` | `0 8px 20px rgba(117,204,0,0.24)` | 主要按钮 |

- 页面内常规列表行不使用阴影。
- 同一层级多个相邻卡片使用边框和间距区分，不为每张卡片叠加重阴影。
- Android 使用 `elevation` 时以视觉结果匹配为准，不直接把阴影模糊值当作 elevation。

## 11. 动效规范

### 11.1 时长 token

| Token | 时长 | 用途 |
| --- | ---: | --- |
| `motion-instant` | 120ms | 按压反馈、图标状态 |
| `motion-fast` | 200ms | 开关、筛选、轻量显隐 |
| `motion-standard` | 300ms | 面板、列表和页面局部转换 |
| `motion-focus` | 600ms | 3D 镜头推进、跨状态 Smart Animate |
| `motion-long` | 800ms | 首次模型加载完成或复杂教学演示 |

### 11.2 缓动

- 标准缓动：`ease-in-out`。
- 元素进入：`ease-out`。
- 元素退出：`ease-in`。
- 不使用弹跳、过度回弹或持续漂浮动画。

### 11.3 页面与面板动效

- 一级页面切换使用系统导航过渡，时长 300ms 左右。
- 底部面板从底部进入，背景遮罩在 200ms 内渐显。
- 选中肌肉时先开始镜头移动，进度约 35% 时启动肌肉高亮。
- 动作筛选面板在镜头锁定后进入，不能与镜头动画同时抢占焦点。
- 用户连续选择肌肉时取消上一动画，以最后一次选择为准。
- 开启“减少动态效果”后，镜头切换缩短为 200ms 淡入淡出，不使用大幅缩放。

## 12. 图标、图片与视频

### 12.1 图标

- 优先使用 Lucide 或产品统一图标库，不手工拼接线条图标。
- 常规线宽：1.8–2px。
- 导航图标：20–24px；列表图标：18–20px；输入框图标：18px。
- 图标按钮必须提供无障碍名称；不熟悉的图标在长按或悬停时提供说明。
- 同一操作在不同页面必须使用同一图标。

### 12.2 动作媒体

- 动作卡片缩略图比例为 1:1 或 4:3，同一列表内不可混用。
- 动作详情主媒体比例为 16:9。
- 封面必须清楚展示起始或关键动作姿势，不能只使用模糊氛围图。
- 视频默认静音，用户明确播放后才加载音轨。
- 视频加载失败时展示动作名称、关键步骤和重试入口。

### 12.3 人体模型素材

- 首版使用正反面人体位图或分层矢量素材，必须叠加独立可点击热区、选中描边、名称和完整解剖路径。
- 不得把整张 UI 截图直接当作人体资源；人体素材必须从界面中独立导出，并支持不同屏幕尺寸清晰缩放。
- 男性、女性素材保持相同画布基准、肌肉 ID 和高亮逻辑。
- 肌腱、韧带、起止点可展示，但不可误标为可孤立训练目标。

## 13. 无障碍规范

- 普通正文与背景对比度至少 4.5:1；大号文字至少 3:1。
- 黄绿色按钮必须使用黑色文字，禁止使用白色文字。
- 洋红肌肉高亮必须同时提供轮廓、标签或准星。
- 点击热区最小 44 × 44px。
- 支持系统字体放大到 130%；页面允许换行和纵向滚动。
- 计时结束、训练完成和错误状态需要视觉反馈与触觉反馈，不能只依赖声音。
- 图表必须提供可被读屏读取的文字摘要。
- 动作视频需要字幕或对应的文字步骤。

## 14. Figma 文件规范

### 14.1 页面结构

Figma 文件按以下页面顺序组织：

```text
00 · Cover & Index
01 · Foundations
02 · Components
03 · Auth & Onboarding
04 · Anatomy 3D
05 · Exercise Library
06 · Training Plans
07 · Workout Session
08 · History & Trends
09 · Nutrition
10 · Profile & Settings
11 · System States
12 · Prototype Flows
```

### 14.2 变量集合

```text
Color / Semantic
Color / Nutrition
Spacing
Radius
Typography
Motion
```

- 颜色变量使用本文 token 名称，不使用 `Green 1`、`Dark Gray 2` 等无语义名称。
- 数值变量需要标注适用范围，例如 `spacing/20`、`radius/card`。
- 首版只有 Dark 模式。不要创建没有业务需求的 Light 模式占位。

### 14.3 组件命名

```text
Button/Primary
Button/Icon
Input/Text
Input/Phone
Control/FilterPill
Control/Segmented
Control/Switch
Navigation/Bottom
Navigation/MusclePath
Card/MuscleSummary
Card/NutritionMetric
Row/Exercise
Row/WorkoutSet
Sheet/Bottom
Feedback/Toast
Feedback/EmptyState
```

### 14.4 组件变体

- 状态属性统一命名为 `State=Default|Pressed|Disabled|Loading|Error`。
- 尺寸属性统一命名为 `Size=Small|Medium|Large`。
- 图标位置使用 `Icon=None|Leading|Trailing|Only`。
- 主题仅使用 `Theme=Dark`，不要创建无法维护的伪 Light 变体。
- 组件内文字通过 Text Property 暴露；图标通过 Instance Swap 暴露。

### 14.5 图层命名

- 页面 Frame：`模块/页面/状态`，例如 `Anatomy/Explore/Default`。
- 区域 Frame：`Section/Header`、`Section/Content`、`Section/BottomNav`。
- 不保留 `Frame 123`、`Group 45`、`Rectangle 8` 等默认名称。
- 3D 舞台占位层命名为 `UnityViewport`，并在说明中标注由 Unity 渲染。

### 14.6 Auto Layout

- 所有表单、列表、卡片内容、导航和按钮必须使用 Auto Layout。
- 只允许 3D 舞台覆盖层、准星和绝对定位浮动工具使用绝对定位。
- 文本容器必须设置合理的 Fill/Hug 规则，禁止零宽文本或依赖手工换行。
- 重复列表项必须创建组件实例，不复制为独立散乱图层。

### 14.7 原型连线

- 核心流程使用 Smart Animate，时长取自动效 token。
- 每个页面至少提供默认、加载、空、错误四种状态中的适用状态。
- 原型起点命名为 `Flow / 新用户`、`Flow / 人体选肌`、`Flow / 完成训练`、`Flow / 记录饮食`。
- 不把所有页面连成一条难以验证的超长流程；按任务拆分原型起点。

## 15. 平台适配规则

### 15.1 iOS

- 遵循系统安全区、返回手势和键盘行为。
- 短暂反馈可使用轻触觉，不频繁震动。
- 权限请求前先显示业务解释页，再调用系统权限弹窗。

### 15.2 Android

- 适配系统返回键和预测返回手势。
- 模糊效果性能不足时使用对应不透明表面色回退。
- 适配不同厂商状态栏和底部手势区域。
- 不依赖 Google Play 服务完成中国区关键流程。

### 15.3 React Native 实现

- 设计 token 维护为单一主题对象，禁止页面内硬编码颜色和间距。
- Noto Sans 作为受控字体资源加载，字体未就绪前避免显示会发生跳动的关键页面。
- 3D 视口与 React Native 覆盖层使用稳定边界，Unity 加载不能改变周边布局尺寸。
- 模糊效果必须提供纯色回退 token。

## 16. 文案规范

- 按钮使用明确动词，例如“开始训练”“记录食物”“查看全部动作”。
- 避免“点击这里”“确定一下”等无信息文案。
- 错误信息说明问题和下一步，例如“模型资源加载失败，可使用基础模型继续”。
- 训练安全内容避免绝对承诺，例如不用“完全孤立胸大肌上束”，改用“重点刺激胸大肌上束”。
- 单位与数字之间保留空格：`60 kg`、`90 秒`、`2,150 kcal`。
- 时间统一使用 24 小时制；日期在中文环境使用 `2026年8月14日` 或 `8月14日`。
- 首次出现专业解剖名称时同时给出通俗说明。

## 17. 设计交付要求

- 每个核心页面提供默认、加载、空、错误和必要权限状态。
- 页面标注使用到的组件、变量和交互目标。
- 设计评审必须包含 390 × 844 与 360 × 800 两个尺寸截图。
- 3D 页面额外检查模型非空、完整入镜、浮动控件不遮挡和横向旋转可达。
- 开发交付前检查所有图层名称、组件实例、变量绑定和原型连线。
- 新页面若偏离本规范，必须在设计评审中说明原因，并同步修改本文件。
